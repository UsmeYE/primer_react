const Contenedor = ({ children }) => {
    return (
      <div style={{ border: '2px solid grey', padding: '10px', margin: '20px' }}>
        {children}
      </div>
    )
  }

export default Contenedor